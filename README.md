# Research Tool

一个智能研究工具，可从多个来源（YouTube、Bilibili、Reddit、贴吧、文章）提取内容，并使用 Qwen3-max 进行深度多阶段研究。

## 服务流程与 AI 应用

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Research Tool 工作流程
└─────────────────────────────────────────────────────────────────────────┘

📥 用户输入链接
    │
    ├─ YouTube 视频链接
    ├─ Bilibili 视频链接  
    ├─ 文章链接
    └─ 评论链接（YouTube/Bilibili/Reddit/贴吧）
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 🔍 内容提取阶段（并行处理）
│
│   ├─ YouTube 字幕提取（使用Playwright抓取）
│   ├─ Bilibili 字幕提取（使用Cookies进行视频下载+阿里云上传+Paraformer转录）
│   ├─ 文章内容提取
│   └─ 评论提取（支持分页）
└─────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 📊 阶段 0: 数据准备
│
│   🤖 AI: Qwen-flash
│   └─ 快速生成内容摘要 Markers: Facts，Opinions, Data，提高后续处理效率
└─────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 🎭 阶段 0.5: 角色生成
│
│   🤖 AI: Qwen3-max
│   └─ 分析内容特征，自动生成最适合的研究角色
└─────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 🔎 阶段 1: 发现目标
│
│   🤖 AI: Qwen3-max
│   └─ 按照内容摘要，智能识别研究目标和关键问题
└─────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 💡 阶段 2: 问题综合
│
│   🤖 AI: Qwen3-max
│   └─ 综合生成结构化研究问题
│   👤 用户可提供输入和反馈
└─────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 🔬 阶段 3: 详细研究
│
│   🤖 AI: Qwen3-max
│   ├─ 多轮深度对话研究
│   ├─ 上下文检索机制
│   └─ 智能内容检索和分析
└─────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 📝 阶段 4: 最终综合
│
│   🤖 AI: Qwen3-max
│   └─ 整合所有研究成果，生成高质量研究报告
└─────────────────────────────────────────────────────────────────────────┘
    │
    ▼
📄 输出研究报告（Markdown 格式）

┌─────────────────────────────────────────────────────────────────────────┐
│                          AI 模型使用策略
├─────────────────────────────────────────────────────────────────────────┤
│
│  ⚡ Qwen-flash  (快速任务)
│     └─ 阶段 0: 数据摘要（快速、高效）
│
│  🧠 Qwen3-max   (深度任务)
│     ├─ 阶段 0.5: 角色生成（需要深度理解）
│     ├─ 阶段 1: 目标发现（需要推理能力）
│     ├─ 阶段 2: 问题综合（需要结构化思维）
│     ├─ 阶段 3: 详细研究（需要多轮对话）
│     └─ 阶段 4: 报告生成（需要综合能力）
│
└─────────────────────────────────────────────────────────────────────────┘
```

## 功能特性

### 内容提取
- **YouTube**: 提取视频字幕（不含时间戳）
- **Bilibili**: 提取中文字幕，支持视频下载和转录备用方案
- **YouTube 评论**: 提取 YouTube 视频评论
- **Bilibili 评论**: 提取 Bilibili 视频评论，支持分页
- **文章**: 提取网页文章和页面的文本内容
- **Reddit**: 提取 Reddit 帖子和评论
- **贴吧**: 提取百度贴吧内容（中文论坛）

### 研究能力
- **多阶段深度研究**: 自动化研究流程，包含 5+ 个阶段
  - 阶段 0: 数据准备和摘要
  - 阶段 0.5: 基于数据生成角色
  - 阶段 1: 发现和目标识别
  - 阶段 2: 研究问题综合
  - 阶段 3: 详细研究执行
  - 阶段 4: 最终综合和报告生成
- **会话管理**: 跟踪研究会话，支持进度追踪
- **进度追踪**: 恢复中断的研究会话
- **质量评估**: 自动数据质量评分和标记

### Web 应用特性
- **前后端分离架构**: FastAPI 后端 + React 前端
- **实时进度更新**: WebSocket 实时推送抓取和研究进度
- **工作流管理**: 完整的抓取→研究→报告生成流程
- **历史记录**: 查看和管理所有研究会话
- **响应式设计**: 支持桌面和移动设备
- **现代化 UI**: 基于 Tailwind CSS 的美观界面

### 技术特性
- **并行处理**: 同时从多个 URL 提取内容
- **智能缓存**: 避免重复提取相同的 URL
- **丰富元数据**: 提取标题、作者、发布日期、字数等
- **Qwen3 集成**: 使用 Qwen3-max、Qwen-flash 等模型进行 AI 驱动的研究
- **媒体工具**: MP4 → MP3 转换和视频下载支持
- **浏览器自动化**: 基于 Playwright 的爬取，支持 Chrome 集成

## 快速开始

### 方式一：使用 Web 界面（推荐）

1. **安装后端依赖:**
   ```bash
   pip install -r requirements.txt
   playwright install chromium
   ```

2. **安装前端依赖:**
   ```bash
   cd client
   npm install
   cd ..
   ```

3. **配置 API 密钥:**
   
   在项目根目录的 `config.yaml` 中配置：
   ```yaml
   qwen:
     api_key: "YOUR_QWEN3_API_KEY"
   ```

4. **启动后端服务:**
   ```bash
   cd backend
   python run_server.py
   # 或
   uvicorn app.main:app --reload --host 0.0.0.0 --port 3001
   ```
   
   后端将在 `http://localhost:3001` 运行

5. **启动前端应用:**
   ```bash
   cd client
   npm run dev
   ```
   
   前端将在 `http://localhost:3000` 运行

6. **使用应用:**
   - 在浏览器中打开 `http://localhost:3000`
   - 输入链接并开始研究

### 方式二：使用命令行

1. **安装依赖:**
   ```bash
   pip install -r requirements.txt
   playwright install chromium
   ```

2. **在 `config.yaml` 中配置 API 密钥:**
   ```yaml
   qwen:
     api_key: "YOUR_QWEN3_API_KEY"
   ```

3. **抓取内容:**
   ```bash
   python tests/test_all_scrapers_and_save_json.py
   ```

4. **运行研究代理:**
   ```bash
   python scripts/run_research.py {batch_id} --topic "您的研究主题"
   ```

详细说明请参阅[使用说明](#使用说明)部分。

## 安装

### 后端安装

1. 安装 Python 3.9 或更高版本
2. （推荐）创建虚拟环境
   ```bash
   python -m venv .venv
   .venv\\Scripts\\activate  # Windows PowerShell
   ```
3. 安装 Python 依赖
   ```bash
   pip install -r requirements.txt
   playwright install chromium
   ```
4. （可选，用于音频转换和 Bilibili 转录）安装 FFmpeg 并确保 `ffmpeg` 在您的 PATH 中
5. （可选，用于 Bilibili 云端转录）在 `config.yaml` 中配置阿里云 OSS 凭证
6. 创建并配置 `config.yaml`（见下文）

### 前端安装

1. 安装 Node.js 18+ 和 npm/yarn/pnpm
2. 安装前端依赖
   ```bash
   cd client
   npm install
   ```

### 主要依赖

**后端:**
- **FastAPI**: 现代、快速的 Web 框架
- **Playwright**: 用于网页爬取的浏览器自动化
- **yt-dlp**: 视频下载（用于 Bilibili）
- **dashscope**: 阿里云 Paraformer API（用于云端转录）
- **openai**: 兼容 OpenAI 的 SDK，用于 Qwen API
- **pytest**: 测试框架

**前端:**
- **React 18**: UI 框架
- **TypeScript**: 类型安全
- **Vite**: 构建工具
- **Tailwind CSS**: 样式框架
- **Zustand**: 状态管理
- **React Router**: 路由管理
- **Axios**: HTTP 客户端

## 配置

在项目根目录创建 `config.yaml`。请参阅仓库中的 `config.yaml` 获取完整示例。

### 主要配置部分

```yaml
qwen:
  api_key: "YOUR_QWEN3_API_KEY"  # 必需：从阿里云 DashScope 获取
  model: "qwen3-max"  # 选项：qwen3-max, qwen-plus, qwen-turbo, qwen-flash
  temperature: 0.7
  max_tokens: 32000

scrapers:
  youtube:
    headless: false
    timeout: 15000
    num_workers: 3
  
  bilibili:
    headless: false
    timeout: 60000
    # Paraformer 云端转录的 OSS 配置
    oss_access_key_id: 'YOUR_OSS_KEY'
    oss_access_key_secret: 'YOUR_OSS_SECRET'
    oss_bucket: 'YOUR_BUCKET'
    cookies:  # 用于认证访问的 Bilibili cookies
      SESSDATA: '...'
  
  bilibili_comments:
    max_comments_per_page: 20
    max_pages: 30
    sort_mode: 3  # 3 = 热门评论, 2 = 最新评论
  
  article:
    method_preference: 'playwright'  # 或 'trafilatura'
    min_content_words: 50

browser:
  connect_to_existing_browser: false
  browser_cdp_url: 'http://localhost:9222'  # 用于 Chrome 调试

research:
  summarization:
    enabled: true
    model: "qwen-flash"  # 快速摘要模型
  retrieval:
    window_words: 20000
    max_followups_per_step: 5
  phases:
    use_marker_overview: true
  synthesis:
    min_words_total: 5000

storage:
  cache_dir: "data/cache"
  base_dir: "data/research"

web:
  host: "127.0.0.1"
  port: 5000
```

## 使用说明

### Web 界面使用

#### 1. 输入链接

在首页输入多个链接（每行一个），支持：
- YouTube 视频链接
- Bilibili 视频链接
- 文章链接
- Reddit 链接
- 贴吧链接

系统会自动识别链接类型并格式化。

#### 2. 开始抓取

点击"开始抓取"后，系统会：
- 实时显示每个链接的抓取进度
- 通过 WebSocket 推送进度更新
- 显示抓取状态（进行中/成功/失败）

#### 3. 开始研究

抓取完成后，可以：
- 输入研究主题
- 开始多阶段研究流程
- 实时查看研究进度
- 在阶段 2 提供用户输入（如需要）

#### 4. 查看结果

研究完成后可以：
- 查看最终报告
- 查看历史记录
- 恢复之前的会话

### 命令行使用

#### 步骤 1: 抓取内容

首先，从 URL 抓取内容并保存到批次中。抓取的数据保存在 `tests/results/run_{batch_id}/` 目录中。

```bash
# 运行爬虫（示例见 tests/ 目录）
python tests/test_all_scrapers_and_save_json.py
```

或使用单独的爬虫：

```python
from scrapers.youtube_scraper import YouTubeScraper
from scrapers.bilibili_scraper import BilibiliScraper
from scrapers.article_scraper import ArticleScraper

# 抓取 YouTube 视频
youtube = YouTubeScraper()
result = youtube.scrape("https://www.youtube.com/watch?v=VIDEO_ID")

# 抓取 Bilibili 视频
bilibili = BilibiliScraper()
result = bilibili.scrape("https://www.bilibili.com/video/BVxxxxx")

# 抓取文章
article = ArticleScraper()
result = article.scrape("https://example.com/article")
```

#### 步骤 2: 运行研究代理

抓取完成后，在抓取的数据批次上运行深度研究代理：

```bash
# 在批次上运行研究代理
python scripts/run_research.py {batch_id} [--topic "研究主题"] [--session {session_id}]

# 示例:
python scripts/run_research.py 20251104_023548 --topic "游戏设计分析"
```

研究代理将：
1. 加载并摘要抓取的数据
2. 基于数据生成研究角色
3. 发现研究目标
4. 综合研究问题
5. 执行详细研究
6. 生成最终报告

研究会话保存在 `data/research/sessions/`，报告保存在 `data/research/reports/`。

#### 恢复研究会话

要恢复中断的研究会话：

```bash
python scripts/run_research.py {batch_id} --session {session_id}
```

### 使用单独的爬虫（Python API）

```python
from scrapers.youtube_scraper import YouTubeScraper
from scrapers.bilibili_comments_scraper import BilibiliCommentsScraper
from scrapers.article_scraper import ArticleScraper

# 提取 YouTube 字幕
youtube = YouTubeScraper()
result = youtube.scrape("https://www.youtube.com/watch?v=VIDEO_ID")

# 提取 Bilibili 评论
comments = BilibiliCommentsScraper()
result = comments.scrape("https://www.bilibili.com/video/BVxxxxx")

# 提取文章内容
article = ArticleScraper()
result = article.scrape("https://example.com/article")
```

## API 文档

后端 API 运行在 `http://localhost:3001`，提供以下端点：

### 链接管理
- `POST /api/links/format` - 格式化 URL 并创建批次
- `GET /api/links/batches/{batch_id}/status` - 获取批次抓取状态

### 工作流
- `POST /api/workflow/start` - 启动工作流
- `GET /api/workflow/status/{workflow_id}` - 获取工作流状态
- `POST /api/workflow/cancel` - 取消正在运行的工作流

### 研究
- `POST /api/research/user_input` - 在研究阶段提交用户输入

### 会话
- `GET /api/sessions/{session_id}` - 获取会话数据
- `GET /api/sessions/{session_id}/steps` - 获取会话步骤

### 报告
- `GET /api/reports/{batch_id}` - 获取批次报告
- `GET /api/reports/{batch_id}/markdown` - 获取 Markdown 格式报告

### 历史记录
- `GET /api/history` - 获取所有研究会话列表
- `GET /api/history/{batch_id}` - 获取特定批次的历史记录
- `DELETE /api/history/{batch_id}` - 删除批次历史记录

### WebSocket
- `WS /ws/{batch_id}` - WebSocket 连接，用于实时进度更新

### API 文档界面
- Swagger UI: `http://localhost:3001/docs`
- ReDoc: `http://localhost:3001/redoc`

## 项目结构

```
research-tool/
├── backend/                      # FastAPI 后端
│   ├── app/
│   │   ├── main.py              # FastAPI 应用入口
│   │   ├── routes/               # API 路由
│   │   │   ├── links.py         # 链接管理
│   │   │   ├── workflow.py      # 工作流管理
│   │   │   ├── research.py      # 研究相关
│   │   │   ├── session.py       # 会话管理
│   │   │   ├── reports.py       # 报告查看
│   │   │   └── history.py       # 历史记录
│   │   ├── services/             # 业务逻辑
│   │   │   ├── link_formatter_service.py
│   │   │   ├── workflow_service.py
│   │   │   ├── scraping_service.py
│   │   │   ├── progress_service.py
│   │   │   └── websocket_ui.py
│   │   └── websocket/            # WebSocket 处理
│   │       └── manager.py
│   ├── lib/                      # 工作流函数重导出
│   ├── run_server.py             # 服务器启动脚本
│   └── README.md                  # 后端文档
├── client/                        # React 前端
│   ├── src/
│   │   ├── components/           # UI 组件
│   │   │   ├── common/          # 通用组件
│   │   │   ├── layout/          # 布局组件
│   │   │   ├── progress/        # 进度组件
│   │   │   └── workflow/        # 工作流组件
│   │   ├── pages/                # 页面组件
│   │   │   ├── LinkInputPage.tsx
│   │   │   ├── ScrapingProgressPage.tsx
│   │   │   ├── ResearchAgentPage.tsx
│   │   │   ├── Phase3SessionPage.tsx
│   │   │   ├── FinalReportPage.tsx
│   │   │   └── HistoryPage.tsx
│   │   ├── stores/               # Zustand 状态管理
│   │   ├── services/             # API 服务
│   │   ├── hooks/                # React Hooks
│   │   └── utils/                # 工具函数
│   ├── package.json
│   └── README.md                  # 前端文档
├── scrapers/                      # 内容爬虫
│   ├── base_scraper.py           # 基础爬虫类
│   ├── youtube_scraper.py         # YouTube 字幕提取
│   ├── youtube_comments_scraper.py  # YouTube 评论
│   ├── bilibili_scraper.py        # Bilibili 字幕提取
│   ├── bilibili_comments_scraper.py  # Bilibili 评论
│   ├── bilibili_downloader.py     # Bilibili 视频下载器
│   ├── article_scraper.py         # 文章内容提取
│   ├── reddit_scraper.py          # Reddit 帖子/评论
│   └── tieba_scraper.py           # 百度贴吧爬虫
├── research/                      # 深度研究代理
│   ├── agent.py                   # 主要研究代理编排器
│   ├── client.py                  # Qwen API 客户端
│   ├── session.py                 # 研究会话管理
│   ├── data_loader.py             # 数据加载工具
│   ├── progress_tracker.py       # 进度追踪
│   ├── retrieval_handler.py       # 内容检索处理器
│   ├── phases/                    # 研究阶段
│   │   ├── phase0_prepare.py      # 数据准备
│   │   ├── phase0_5_role_generation.py  # 角色生成
│   │   ├── phase1_discover.py     # 发现阶段
│   │   ├── phase2_synthesize.py   # 问题综合
│   │   ├── phase3_execute.py      # 研究执行
│   │   └── phase4_synthesize.py   # 最终综合
│   ├── prompts/                   # 提示模板
│   ├── summarization/             # 摘要工具
│   └── ui/                        # UI 组件
├── core/                           # 核心功能
│   ├── config.py                  # 配置管理
│   └── __init__.py
├── data/                           # 数据存储
│   ├── cache/                     # URL 缓存存储
│   ├── cookies/                   # 浏览器 cookies
│   └── research/                  # 研究输出
│       ├── sessions/              # 研究会话文件
│       └── reports/                # 生成的报告
├── docs/                           # 文档
│   ├── analysis/                  # 分析文档
│   ├── implementation/            # 实现文档
│   ├── installation/              # 安装指南
│   ├── debug/                     # 调试笔记
│   └── planning/                  # 规划文档
├── downloads/                      # 下载的媒体文件
├── scripts/                        # 工具脚本
│   ├── run_research.py            # 主要研究入口点
│   ├── check_chrome_connection.py # 浏览器调试
│   └── start_chrome_with_debugging.bat  # Chrome 调试
├── tests/                          # 测试文件
│   ├── test_*.py                  # 单独爬虫测试
│   ├── test_full_workflow_integration.py  # 集成测试
│   └── results/                   # 测试结果
├── utils/                          # 工具
│   ├── convert_mp4_to_mp3.py     # 媒体转换
│   └── link_formatter.py         # 链接格式化
├── config.yaml                     # 配置文件
├── requirements.txt                # Python 依赖
└── README.md                       # 本文件
```

## 测试

### 运行单独的爬虫测试

```bash
# 测试 YouTube 爬虫
pytest tests/test_youtube_scraper.py -v

# 测试 Bilibili 爬虫
pytest tests/test_bilibili_scraper.py -v

# 测试 Bilibili 评论爬虫
pytest tests/test_bilibili_comments_scraper.py -v

# 测试文章爬虫
pytest tests/test_article_scraper.py -v

# 测试 Reddit 爬虫
pytest tests/test_reddit_scraper.py -v
```

### 运行集成测试

```bash
# 测试完整工作流集成
pytest tests/test_full_workflow_integration.py -v

# 运行所有测试
pytest -q
```

### 测试特定功能

```bash
# 测试链接格式化器
pytest tests/test_link_formatter.py -v

# 测试进度追踪
pytest tests/test_progress_tracking.py -v

# 使用 -k 按关键字选择测试
pytest -q -k "bilibili"  # 运行所有 Bilibili 相关测试
```

**注意事项:**
- 某些集成测试可能会执行实时网络请求，需要有效的 `config.yaml` 凭证。
- 测试可能需要浏览器 cookies 进行认证爬取（例如 Bilibili）。
- 某些测试可能会下载视频/音频文件用于转录测试。

## 故障排除

### 浏览器问题

**Playwright 无法启动:**
```bash
playwright install chromium
```

**Chrome 连接问题:**
- 使用 `scripts/check_chrome_connection.py` 测试浏览器连接
- 用于调试，使用以下命令启动 Chrome: `chrome.exe --remote-debugging-port=9222 --user-data-dir="C:\temp\chrome-profile"`
- 在 `config.yaml` 中设置 `browser.connect_to_existing_browser: true`

### 速率限制

**Bilibili/YouTube 速率限制:**
- 在 `config.yaml` 中减少特定爬虫的 `num_workers`
- 增加请求之间的延迟
- 使用带 cookies 的认证会话

### 转录问题

**Bilibili 转录失败:**
- 验证 FFmpeg 已安装: `ffmpeg -version`
- 检查 `config.yaml` 中的 OSS 配置以进行 Paraformer 云端转录
- 验证视频下载目录权限

**音频转换失败:**
- 验证 FFmpeg 已安装并在 PATH 中可用
- 检查 `downloads/` 目录权限
- 确保有足够的磁盘空间

### API 问题

**Qwen API 错误:**
- 验证 `config.yaml` 中的 API 密钥是否正确
- 检查 API 配额和速率限制
- 确保网络连接到 DashScope API
- 检查模型名称是否正确（qwen3-max、qwen-plus 等）

**API 超时错误:**
- 在 `config.yaml` 中增加超时值
- 减少 `max_tokens` 或内容大小
- 检查网络稳定性

### 研究代理问题

**会话恢复失败:**
- 验证会话文件是否存在于 `data/research/sessions/`
- 检查会话文件格式是否为有效的 JSON
- 确保批次 ID 与现有数据匹配

**研究期间的内存问题:**
- 在 `config.yaml` 的 research.retrieval 部分减少 `window_words`
- 减少 `max_followups_per_step`
- 处理较小的数据批次

### Web 应用问题

**后端无法启动:**
- 检查端口 3001 是否被占用
- 查看 `logs/backend.log` 获取详细错误信息
- 确保所有 Python 依赖已正确安装

**前端无法启动:**
- 检查 Node.js 版本（需要 18+）
- 删除 `node_modules` 并重新运行 `npm install`
- 检查端口 3000 是否被占用

**WebSocket 连接失败:**
- 确保后端服务正在运行
- 检查浏览器控制台是否有 CORS 错误
- 验证后端 WebSocket 端点配置正确

### 一般问题

**导入错误:**
- 确保虚拟环境已激活
- 重新安装依赖: `pip install -r requirements.txt`
- 检查 Python 版本（需要 3.9+）

**权限错误:**
- 检查 `data/`、`downloads/`、`storage/` 的目录权限
- 确保缓存和会话文件的写入权限

## 开发指南

### 后端开发

后端使用 FastAPI，采用简化的架构设计：
- 直接使用 `tests/` 文件夹中经过验证的工作代码
- 添加 WebSocket 集成以实现实时更新
- 保持简单性，避免重复实现

详细架构说明请参阅 `backend/README.md`。

### 前端开发

前端使用 React + TypeScript + Vite：
- 组件化设计，易于维护
- Zustand 进行状态管理
- Tailwind CSS 进行样式设计
- WebSocket 实时通信

详细说明请参阅 `client/README.md`。

### 添加新功能

1. **在 tests/ 中实现** - 首先在测试文件中添加功能
2. **测试验证** - 使用测试脚本验证功能
3. **添加到后端** - 在后端服务中导入并使用
4. **添加 WebSocket 回调** - 如需要实时更新
5. **更新前端** - 添加相应的 UI 组件和页面

## 许可证

MIT
